# Tauri-generated schema files (re-derived on each build).
gen/

# Platform-specific icon variants generated by `cargo tauri icon`.
# We keep only the source icons checked in (32x32.png, 128x128.png,
# 128x128@2x.png, icon.icns, icon.ico, icon.png).
icons/Square*.png
icons/StoreLogo.png
icons/64x64.png
icons/android/
icons/ios/
